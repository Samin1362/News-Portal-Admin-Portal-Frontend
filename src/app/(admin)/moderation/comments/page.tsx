import { PhasePlaceholder } from "@/components/overview/PhasePlaceholder";

export default function CommentsPage() {
  return (
    <PhasePlaceholder
      title="Comments"
      phase="Phase 5"
      description="Approve / reject / hard-delete queue with bulk actions, keyboard shortcuts, and per-comment detail drawer listing all reports."
    />
  );
}
