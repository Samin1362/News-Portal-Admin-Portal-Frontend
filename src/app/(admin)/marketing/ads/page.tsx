import { PhasePlaceholder } from "@/components/overview/PhasePlaceholder";

export default function AdsPage() {
  return (
    <PhasePlaceholder
      title="Advertisements"
      phase="Phase 7"
      description="CRUD with placement-aware live preview (728×90 header, 300×250 sidebar, full-width inline), active toggle, impression / click / CTR per row."
    />
  );
}
