import { PhasePlaceholder } from "@/components/overview/PhasePlaceholder";

export default function ApiKeysPage() {
  return (
    <PhasePlaceholder
      title="API & Webhooks"
      phase="Phase 9 placeholder"
      description="Backend ships no token issuance today. Page reserved for when the platform exposes API tokens + webhook endpoints — for now, contact a platform admin."
    />
  );
}
