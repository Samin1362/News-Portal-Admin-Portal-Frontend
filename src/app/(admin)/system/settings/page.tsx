import { PhasePlaceholder } from "@/components/overview/PhasePlaceholder";

export default function SettingsPage() {
  return (
    <PhasePlaceholder
      title="Settings"
      phase="Phase 9"
      description="Own-profile form (PATCH /users/me), density preference, default landing page, ticker toggle. Send-test-email button validates the Resend setup."
    />
  );
}
