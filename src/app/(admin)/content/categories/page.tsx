import { PhasePlaceholder } from "@/components/overview/PhasePlaceholder";

export default function CategoriesPage() {
  return (
    <PhasePlaceholder
      title="Categories"
      phase="Phase 6"
      description="Drag-handle ordered list, banner uploader via Cloudinary, isActive toggle, slug-collision handling. Backend blocks delete if articles still reference."
    />
  );
}
