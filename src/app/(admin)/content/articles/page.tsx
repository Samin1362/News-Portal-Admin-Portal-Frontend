import { PhasePlaceholder } from "@/components/overview/PhasePlaceholder";

export default function ArticlesPage() {
  return (
    <PhasePlaceholder
      title="All articles"
      phase="Phase 4"
      description="Cross-author article table with status filter chips, search by headline / slug, per-row flags toggle (breaking / featured / trending), and override-edit links."
    />
  );
}
