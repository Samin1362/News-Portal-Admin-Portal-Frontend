import { PhasePlaceholder } from "@/components/overview/PhasePlaceholder";

export default function TagsPage() {
  return (
    <PhasePlaceholder
      title="Tags"
      phase="Phase 6"
      description="Chip-input tag creator + usage-count column derived from /articles/queue meta.total per tag. Delete blocked when articles still reference."
    />
  );
}
